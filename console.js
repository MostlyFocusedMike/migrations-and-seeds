// this file is just to play around with the Node REPL console
// they are global intentionally, never use globals in an app.
// This is just me very much missing Rails Console. It was neat.

const msg = 'message';
User = require('./src/pure-models/pure-user');

